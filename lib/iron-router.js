Router.map(function () {
  this.route('HOME', {
    path: '/' 
  });
   this.route('PRESENTATION', {
    path: '/' 
  });
 
  this.route('PROGRAM', {
    path: '/' 
  });
    this.route('PARTICIPANTS', {
   path: '/PARTICIPANTS' 
  });
     
    this.route('PARTNERS', {
    path: '/' 
  });
    this.route('CONTACT', {
    path: '/' 
  });
});